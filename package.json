{
  "description": "Carbon.RangeEditor",
  "license": "proprietary",
  "private": true,
  "scripts": {
    "pipeline": "pnpm install; pnpm build",
    "build": "node build.mjs --production",
    "watch": "node build.mjs --watch",
    "dev": "node build.mjs"
  },
  "devDependencies": {
    "@neos-project/neos-ui-extensibility": "~8.3.11",
    "@stylexjs/esbuild-plugin": "^0.9.3",
    "esbuild": "^0.24.0",
    "prettier": "^3.4.2"
  },
  "dependencies": {
    "@stylexjs/stylex": "^0.9.3",
    "use-debounce": "^10.0.4"
  }
}
